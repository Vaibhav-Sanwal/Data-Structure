#include<stdio.h>

void func1 (int array[],int length)
{
    int sum = 0;
    int product = 1;
    for (size_t i = 0; i < length; i++)
    {
       sum += array[i]; /* code */
    }
    for (size_t i = 0; i < length; i++)
    {
       product*= array[i]; /* code */
    }
       
}
int main(){
    int arr[]={3,5,66};
    func1 (arr,3);
    return 0;
}
